export interface FormData {
    name: string;
    country: string;
    projectCode: string;
    description: string;
    startDate: string;
    endDate: string;
    amount: string;
    currency: string;
}